# Meta OS v1.0 Architecture (Frozen)

## Core Flow
Prelude
→ Ontology / Scene Structuring
→ Judgment Engine
→ DeliveryPlan (artifact)
→ Queue (artifact)
→ External Executor (outside Meta OS)

## Invariants
- Meta OS never executes actions
- All outputs are artifacts
- All execution requires human acceptance + authorization
