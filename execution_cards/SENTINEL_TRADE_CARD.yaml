id: SENTINEL-TRADE-CARD-v1
owner: B1.SENTINEL
kind: execution_card
status: active

requires:
  - LOCK-BOOTSTRAP
  - LOCK-2
  - LOCK-3

scope:
  domain: trading
  venue: crypto
  mode: paper   # paper|live (live는 별도 승인/게이트 필요)
  risk: bounded

gates:
  approval_required: true
  observer_event_required: true
  replay_packet_required: true

limits:
  max_orders_per_hour: 5
  max_notional_usd: 1000
  max_leverage: 1

notes:
  - "This card only defines intent/scope. Execution is controlled by B0 pipeline + approvals."
