# Pull Request

## Required (Fail-Closed)
- Do not remove keys.
- Use ASCII colon ":" (not "：").
- Keep keys at column 0 (no leading spaces) so gates can grep `^KEY:`.

DESIGN_ARTIFACT:
STAGE:

WHY_NOW:
VERIFY:
BREAK_RISK:
